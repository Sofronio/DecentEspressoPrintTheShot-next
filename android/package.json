{
  "name": "printtheshot-next-android",
  "version": "2.1.0-next.1",
  "private": true,
  "description": "PrintTheShot Android shell (Capacitor) with a native Bluetooth ESC/POS printing plugin",
  "scripts": {
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "cap:copy": "cap copy android",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
