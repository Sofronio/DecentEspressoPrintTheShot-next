# Node / Capacitor 依赖与锁文件
# Node / Capacitor dependencies and lockfiles
node_modules/

# `npx cap add android` 生成的原生工程:可再生,不入库
# The native project generated by `npx cap add android`: reproducible, not tracked.
# (锚定在本文件所在目录,所以只忽略 android/android/,不影响 android/app/)
# (Anchored to this directory, so only android/android/ is ignored, never android/app/.)
/android/

# Gradle / Android 构建产物
# Gradle / Android build output
.gradle/
build/
local.properties
captures/
*.iml
.idea/
.DS_Store
